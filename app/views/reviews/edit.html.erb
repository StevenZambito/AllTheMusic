<h1 class="text-center font-bold mt-10 mb-10 text-xl">Edit Review</h1>

<div class="flex justify-center mt-2 mb-2">

  <div class="bg-white border border-black rounded p-5">

    <%= form_for ([@album, @review]) do |f| %>

      <div id="rating-form" class="mb-4 flex">
        <div class="mr-2">
          <%= f.label :rating, :required => true %>
        </div>
      </div>

      <div class="mb-4">
        <div>
          <%= f.label :title %>
        </div>

        <div>
          <%= f.text_field :title, :size => "70", class: "rounded p-1 mt-1 border-2 border-black", :required => true %>
        </div>
      </div>

      <div class="mb-4">
        <div>
          <%= f.label :comment %>
        </div>

        <div>
          <%= f.text_area :comment, :size => "70", class: "rounded p-1 mt-1 border-2 border-black", :required => true %>
        </div>
      </div>

      <%= f.submit class: "my-4 bg-black hover:bg-white hover:text-black border border-black cursor-pointer text-white font-bold py-2 px-4 rounded" %>
      <%= link_to "Back", album_path(@album) %>
    <% end %>

  </div>
  
</div>

<script>
  $('#rating-form').raty({
    path: '/assets/',
    scoreName: 'review[rating]'
  });
</script>